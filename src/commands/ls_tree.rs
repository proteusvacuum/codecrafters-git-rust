pub fn ls_tree(name_only: &bool, path: &str) {
    dbg!(name_only);
    dbg!(path);
}
// We recommend implementing the full ls-tree output too since that'll require that you parse all data in the tree object, not just filenames.
